<UserControl x:Class="GalaXako.Editor.App.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:avalon="http://icsharpcode.net/sharpdevelop/avalonedit"
             Loaded="EditorView_Loaded" DataContextChanged="EditorView_DataContextChanged">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="16" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="{Binding FileName}" Style="{StaticResource PageTitle}" TextTrimming="CharacterEllipsis" /><TextBlock Text="{Binding FilePath}" Style="{StaticResource PageSubtitle}" TextTrimming="CharacterEllipsis" ToolTip="{Binding FilePath}" /></StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"><Border Background="#211B2E" CornerRadius="8" Padding="10,6" Margin="0,0,8,0"><TextBlock Text="{Binding FileSize}" FontSize="12" /></Border><Border Background="#211B2E" CornerRadius="8" Padding="10,6"><TextBlock Text="{Binding EncodingName}" Foreground="{StaticResource Brush.Primary.Soft}" FontSize="12" /></Border></StackPanel>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid>
                <Grid x:Name="EmptyPanel">
                    <Grid.Style><Style TargetType="Grid"><Setter Property="Visibility" Value="Visible" /><Style.Triggers><DataTrigger Binding="{Binding HasFile}" Value="True"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers></Style></Grid.Style>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><TextBlock Text="&#xE7C3;" FontFamily="Segoe Fluent Icons" FontSize="32" Foreground="{StaticResource Brush.Primary.Soft}" HorizontalAlignment="Center" /><TextBlock Text="Dosya seçilmedi" FontSize="18" FontWeight="SemiBold" Margin="0,14,0,6" /><TextBlock Text="Düzenlemek veya incelemek için Ana Sayfa'dan bir dosya seçin." Foreground="{StaticResource Brush.Text.Secondary}" /></StackPanel>
                </Grid>

                <Grid x:Name="NormalPanel">
                    <Grid.Style><Style TargetType="Grid"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsNormalMode}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Grid.Style>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="8" /><RowDefinition Height="Auto" /><RowDefinition Height="8" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                    <WrapPanel>
                        <Button Content="Kaydet" Command="{Binding SaveCommand}" Margin="0,0,8,0" />
                        <Button Content="Farklı Kaydet" Command="{Binding SaveAsCommand}" Margin="0,0,8,0" />
                        <Button Content="Geri Al" Click="Undo_Click" Margin="0,0,8,0" />
                        <Button Content="Yinele" Click="Redo_Click" Margin="0,0,12,0" />
                        <Button Content="Analiz Et" Command="{Binding AnalyzeCommand}" Margin="0,0,12,0" />
                        <CheckBox x:Name="WordWrapCheckBox" Content="Sözcük kaydırma" Checked="WordWrap_Changed" Unchecked="WordWrap_Changed" Margin="0,0,12,0" VerticalAlignment="Center" />
                        <CheckBox x:Name="WhitespaceCheckBox" Content="Boşlukları göster" Checked="Whitespace_Changed" Unchecked="Whitespace_Changed" VerticalAlignment="Center" />
                    </WrapPanel>
                    <WrapPanel Grid.Row="2">
                        <TextBox x:Name="FindBox" Width="180" ToolTip="Bul" Margin="0,0,8,0" />
                        <TextBox x:Name="ReplaceBox" Width="180" ToolTip="Şununla değiştir" Margin="0,0,8,0" />
                        <Button Content="Önceki" Click="FindPrevious_Click" Margin="0,0,8,0" />
                        <Button Content="Sonraki" Click="FindNext_Click" Margin="0,0,8,0" />
                        <Button Content="Değiştir" Click="Replace_Click" Margin="0,0,12,0" />
                        <CheckBox x:Name="CaseSensitiveCheckBox" Content="Büyük/küçük" Margin="0,0,10,0" VerticalAlignment="Center" />
                        <CheckBox x:Name="WholeWordCheckBox" Content="Tam sözcük" Margin="0,0,10,0" VerticalAlignment="Center" />
                        <CheckBox x:Name="RegexCheckBox" Content="Regex" VerticalAlignment="Center" />
                    </WrapPanel>
                    <avalon:TextEditor x:Name="NormalEditor" Grid.Row="4" ShowLineNumbers="True" FontFamily="Cascadia Mono, Consolas" FontSize="{Binding ZoomFontSize}" Background="#0D0D12" Foreground="{StaticResource Brush.Text.Primary}" LineNumbersForeground="{StaticResource Brush.Text.Muted}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" TextChanged="NormalEditor_TextChanged" PreviewKeyDown="NormalEditor_PreviewKeyDown">
                        <avalon:TextEditor.ContextMenu><ContextMenu><MenuItem Header="Kes" Command="ApplicationCommands.Cut" /><MenuItem Header="Kopyala" Command="ApplicationCommands.Copy" /><MenuItem Header="Yapıştır" Command="ApplicationCommands.Paste" /><Separator /><MenuItem Header="Tümünü Seç" Command="ApplicationCommands.SelectAll" /></ContextMenu></avalon:TextEditor.ContextMenu>
                    </avalon:TextEditor>
                    <Grid Grid.Row="5" Margin="0,10,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel Orientation="Horizontal"><TextBlock Text="{Binding LineCount, StringFormat={}{0:N0} satır}" Foreground="{StaticResource Brush.Text.Secondary}" /><TextBlock Text=" · " Foreground="{StaticResource Brush.Text.Muted}" /><TextBlock Text="{Binding CharacterCount, StringFormat={}{0:N0} karakter}" Foreground="{StaticResource Brush.Text.Secondary}" /><TextBlock x:Name="CaretStatus" Text=" · Satır 1, Sütun 1" Foreground="{StaticResource Brush.Text.Secondary}" /></StackPanel><TextBlock Grid.Column="1" Text="{Binding StatisticsText}" Foreground="{StaticResource Brush.Text.Muted}" Margin="16,0" /><StackPanel Grid.Column="2" Orientation="Horizontal"><TextBlock Text="Yakınlaştır" Foreground="{StaticResource Brush.Text.Muted}" Margin="0,0,8,0" VerticalAlignment="Center" /><Slider Width="100" Minimum="9" Maximum="32" Value="{Binding ZoomFontSize}" /></StackPanel></Grid>
                </Grid>

                <Grid x:Name="LargePanel">
                    <Grid.Style><Style TargetType="Grid"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding IsLargeMode}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></Grid.Style>
                    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="8" /><RowDefinition Height="Auto" /><RowDefinition Height="8" /><RowDefinition Height="Auto" /><RowDefinition Height="8" /><RowDefinition Height="Auto" /><RowDefinition Height="8" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                    <WrapPanel>
                        <Button Content="Başlangıç" Command="{Binding BeginningCommand}" Margin="0,0,8,0" /><Button Content="Önceki parça" Command="{Binding PreviousChunkCommand}" Margin="0,0,8,0" /><Button Content="Sonraki parça" Command="{Binding NextChunkCommand}" Margin="0,0,8,0" /><Button Content="Son" Command="{Binding EndCommand}" Margin="0,0,16,0" />
                        <Button Content="0%" Command="{Binding GoToPercentCommand}" CommandParameter="0" Margin="0,0,4,0" /><Button Content="25%" Command="{Binding GoToPercentCommand}" CommandParameter="25" Margin="0,0,4,0" /><Button Content="50%" Command="{Binding GoToPercentCommand}" CommandParameter="50" Margin="0,0,4,0" /><Button Content="75%" Command="{Binding GoToPercentCommand}" CommandParameter="75" Margin="0,0,4,0" /><Button Content="100%" Command="{Binding GoToPercentCommand}" CommandParameter="100" />
                    </WrapPanel>
                    <WrapPanel Grid.Row="2"><TextBlock Text="Satır" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,6,0" /><TextBox Width="120" Text="{Binding GoToLine, UpdateSourceTrigger=LostFocus}" Margin="0,0,6,0" /><Button Content="Git" Command="{Binding GoToLineCommand}" Margin="0,0,18,0" /><TextBlock Text="Bayt ofseti" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,6,0" /><TextBox Width="140" Text="{Binding GoToByte, UpdateSourceTrigger=LostFocus}" Margin="0,0,6,0" /><Button Content="Git" Command="{Binding GoToByteCommand}" Margin="0,0,18,0" /><TextBlock Text="Parça" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,6,0" /><ComboBox Width="120" SelectedValuePath="Tag" SelectedValue="{Binding ChunkLineCount}"><ComboBoxItem Content="1.000 satır" Tag="1000" /><ComboBoxItem Content="5.000 satır" Tag="5000" /><ComboBoxItem Content="10.000 satır" Tag="10000" /><ComboBoxItem Content="50.000 satır" Tag="50000" /></ComboBox><Button Content="Analiz Et" Command="{Binding AnalyzeCommand}" Margin="12,0,0,0" /></WrapPanel>
                    <Grid Grid.Row="4"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><WrapPanel><TextBox Width="260" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" ToolTip="Büyük dosyada ara" Margin="0,0,8,0" /><CheckBox Content="Büyük/küçük" IsChecked="{Binding SearchCaseSensitive}" Margin="0,0,8,0" /><CheckBox Content="Regex" IsChecked="{Binding SearchRegex}" Margin="0,0,8,0" /><Button Content="Ara" Command="{Binding SearchLargeFileCommand}" Margin="0,0,8,0" /><Button Content="İptal" Command="{Binding CancelSearchCommand}" /></WrapPanel><TextBlock Grid.Column="1" Text="{Binding SearchStatus}" Foreground="{StaticResource Brush.Text.Secondary}" VerticalAlignment="Center" /></Grid>
                    <ListBox Grid.Row="6" ItemsSource="{Binding SearchResults}" MaxHeight="120" Background="#0D0D12" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled"><ListBox.ItemTemplate><DataTemplate><Button Command="{Binding DataContext.NavigateSearchResultCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" HorizontalContentAlignment="Stretch" Background="Transparent" BorderThickness="0" Padding="8,4"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="100" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><TextBlock Text="{Binding LineNumber, StringFormat=Satır {0:N0}}" Foreground="{StaticResource Brush.Primary.Soft}" /><TextBlock Grid.Column="1" Text="{Binding Preview}" TextTrimming="CharacterEllipsis" FontFamily="Cascadia Mono, Consolas" /></Grid></Button></DataTemplate></ListBox.ItemTemplate></ListBox>
                    <avalon:TextEditor x:Name="LargePreviewEditor" Grid.Row="8" IsReadOnly="True" ShowLineNumbers="True" FontFamily="Cascadia Mono, Consolas" FontSize="{Binding ZoomFontSize}" Background="#0D0D12" Foreground="{StaticResource Brush.Text.Primary}" LineNumbersForeground="{StaticResource Brush.Text.Muted}" BorderBrush="{StaticResource Brush.Border}" BorderThickness="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
                    <Grid Grid.Row="9" Margin="0,10,0,0"><TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource Brush.Text.Secondary}" /><TextBlock HorizontalAlignment="Center" Text="{Binding StatisticsText}" Foreground="{StaticResource Brush.Text.Muted}" /><TextBlock HorizontalAlignment="Right" Foreground="{StaticResource Brush.Text.Secondary}"><Run Text="Toplam " /><Run Text="{Binding LineCount, Mode=OneWay, StringFormat={}{0:N0}}" /><Run Text=" satır · " /><Run Text="{Binding LineEnding, Mode=OneWay}" /></TextBlock></Grid>
                </Grid>

                <Border Background="#D909090D" CornerRadius="12" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><ProgressBar Width="260" IsIndeterminate="True" /><TextBlock Text="{Binding BusyMessage}" Margin="0,14,0,0" Foreground="{StaticResource Brush.Text.Secondary}" HorizontalAlignment="Center" /></StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
